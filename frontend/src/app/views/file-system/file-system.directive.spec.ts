import { FileSystemDirective } from './file-system.directive';

describe('FileSystemDirective', () => {
  it('should create an instance', () => {
    const directive = new FileSystemDirective();
    expect(directive).toBeTruthy();
  });
});
